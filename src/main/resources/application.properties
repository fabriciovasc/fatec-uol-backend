## Server
server.port = 8080
server.servlet.context-path=/api

## Logging
# Show sql statement
logging.level.org.hibernate.SQL = debug

# Show sql values
logging.level.org.hibernate.type.descriptor.sql = trace

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://uolfatec.cjjj3jdoamen.us-east-1.rds.amazonaws.com:3306/uol?useTimezone=true&serverTimezone=UTC
spring.datasource.username = masterAdmin
spring.datasource.password = uolPass1234

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = validate
